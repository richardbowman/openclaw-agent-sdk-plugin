{
  "name": "openclaw-agent-sdk-plugin",
  "version": "3.0.0",
  "description": "OpenClaw plugin: Claude Agent SDK HTTP server + (dormant) CLI backend. Handles all channels via a persistent HTTP provider with session stitching, HA MCP, and OAuth auth.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "openclaw": {
    "extensions": ["./index.mjs"]
  },
  "peerDependencies": {
    "openclaw": ">=2026.5.12"
  },
  "peerDependenciesMeta": {
    "openclaw": {
      "optional": true
    }
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.142",
    "zod": "^4.0.0"
  }
}
